<%
    const s = stats || {};
    const totalRevenue = parseFloat(s.total_revenue || 0);
    const balance = parseFloat(s.balance || 0);
    const totalSales = parseInt(s.total_sales || 0);
    const totalProducts = parseInt(s.total_products || 0);
    const platformSales = parseFloat(s.platform_sales || 0);
    const userSlug = s.slug || 'username';
%>

<style>
.dash-cards-row {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;
    margin-top: 10px;
}
.dash-stat-card {
    background: #fff;
    border: 1px solid #f1f5f9;
    border-radius: 16px;
    padding: 24px;
    position: relative;
    overflow: hidden;
    transition: all 0.3s ease;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
}
.dash-stat-card:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.08);
}
.dash-stat-card::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 4px;
}
.dash-stat-card:nth-child(1)::before { background: linear-gradient(90deg, #10b981, #dcfce7); }
.dash-stat-card:nth-child(2)::before { background: linear-gradient(90deg, #3b82f6, #dbeafe); }
.dash-stat-card:nth-child(3)::before { background: linear-gradient(90deg, #f59e0b, #fef3c7); }
.dash-stat-card:nth-child(4)::before { background: linear-gradient(90deg, #8b5cf6, #ede9fe); }
.dash-stat-card:nth-child(5)::before { background: linear-gradient(90deg, #6b7280, #f3f4f6); }

.dash-stat-label {
    font-size: 11px;
    font-weight: 800;
    color: #64748b;
    letter-spacing: 0.8px;
    margin-bottom: 12px;
    text-transform: uppercase;
}
.dash-stat-value {
    font-size: clamp(18px, 1.8vw, 26px);
    font-weight: 900;
    color: #111827;
    letter-spacing: -0.5px;
    line-height: 1.2;
    word-break: break-word; /* Ensure it stays inside */
    display: flex;
    align-items: baseline;
    gap: 4px;
}
.dash-stat-value small {
    font-size: 0.6em;
    color: #94a3b8;
    font-weight: 700;
}
.dash-stat-sub {
    font-size: 14px;
    font-weight: 600;
    color: #94a3b8;
}

@media (max-width: 1400px) { .dash-cards-row { grid-template-columns: repeat(3, 1fr); } }
@media (max-width: 1024px) { .dash-cards-row { grid-template-columns: repeat(2, 1fr); } }
@media (max-width: 640px) { .dash-cards-row { grid-template-columns: 1fr; } }

/* BIO LINK CARD ENHANCEMENT */
.bio-card {
    background: #fff;
    border: 1px solid #f1f5f9;
    border-radius: 16px;
    padding: 24px;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
}
.bio-input-group {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-top: 8px;
}
.bio-input-wrap {
    flex: 1;
    display: flex;
    align-items: center;
    background: #f8fafc;
    border: 1.5px solid #e2e8f0;
    border-radius: 12px;
    padding: 10px 18px;
    transition: all 0.2s;
}
.bio-input-wrap:focus-within {
    border-color: #3b82f6;
    background: #fff;
    box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
}
.bio-btn {
    padding: 11px 20px;
    border-radius: 12px;
    font-size: 13px;
    font-weight: 700;
    cursor: pointer;
    transition: all 0.2s;
    display: flex;
    align-items: center;
    gap: 8px;
    border: 1.5px solid #e2e8f0;
    background: #fff;
    color: #475569;
}
.bio-btn:hover { background: #f8fafc; border-color: #cbd5e1; }
.bio-btn.primary { background: #3b82f6; color: #fff; border: none; }
.bio-btn.primary:hover { background: #2563eb; }
</style>

<!-- YOUR BIO LINK -->
<div class="bio-card" style="margin-bottom: 24px;">
    <div style="font-size: 11px; font-weight: 800; color: #64748b; letter-spacing: 1px; margin-bottom: 12px; text-transform: uppercase;">LINK BIO ANDA</div>
    <div class="bio-input-group">
        <div class="bio-input-wrap">
            <span style="color:#94a3b8; font-size:14px; font-weight:600;">lingku.xyz/</span>
            <input type="text" id="slugInput" value="<%= userSlug %>" 
                style="border:none; outline:none; background:transparent; font-size:14px; font-weight:800; color:#111827; flex:1; font-family:inherit;">
        </div>
        <button onclick="updateSlug()" class="bio-btn primary">Update</button>
        <button onclick="window.open('/' + document.getElementById('slugInput').value, '_blank')" class="bio-btn" title="Open Link">
            <i class="fas fa-external-link-alt"></i>
        </button>
        <button onclick="copyLink()" class="bio-btn" title="Copy Link">
            <i class="fas fa-copy" style="color:#10b981;"></i> Copy
        </button>
    </div>
</div>

<!-- STAT CARDS -->
<div class="dash-cards-row">
    <div class="dash-stat-card">
        <div class="dash-stat-label">Total Revenue</div>
        <div class="dash-stat-value"><small>Rp</small><%= totalRevenue.toLocaleString('id-ID') %></div>
    </div>
    <div class="dash-stat-card">
        <div class="dash-stat-label">Saldo Tersedia</div>
        <div class="dash-stat-value"><small>Rp</small><%= balance.toLocaleString('id-ID') %></div>
    </div>
    <div class="dash-stat-card">
        <div class="dash-stat-label">Total Sales (Qty)</div>
        <div class="dash-stat-value"><%= totalSales %> <span class="dash-stat-sub">Items</span></div>
    </div>
    <div class="dash-stat-card">
        <div class="dash-stat-label">Jumlah Produk</div>
        <div class="dash-stat-value"><%= totalProducts %> <span class="dash-stat-sub">Katalog</span></div>
    </div>
    <div class="dash-stat-card">
        <div class="dash-stat-label">Penjualan Platform</div>
        <div class="dash-stat-value"><small>Rp</small><%= platformSales.toLocaleString('id-ID') %></div>
    </div>
</div>

<!-- CHART -->
<div class="bio-card" style="padding: 28px; margin-top: 24px;">
    <div style="font-size: 12px; font-weight: 800; color: #1e293b; letter-spacing: 0.5px; margin-bottom: 24px; display: flex; align-items: center; gap: 8px;">
        <i class="fas fa-chart-line" style="color:#3b82f6;"></i> SALES PERFORMANCE (LAST 7 DAYS)
    </div>
    <div style="height: 340px; position: relative;">
        <canvas id="dashChart"></canvas>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
    const labels = [];
    const dataVals = [];
    <% (chartData || []).forEach(function(d) { %>
        labels.push('<%= new Date(d.date).toLocaleDateString("id-ID", { day:"numeric", month:"short" }) %>');
        dataVals.push(<%= d.revenue || 0 %>);
    <% }); %>

    new Chart(document.getElementById('dashChart').getContext('2d'), {
        type: 'line',
        data: {
            labels: labels,
            datasets: [{
                label: 'Revenue',
                data: dataVals,
                borderColor: '#3b82f6',
                backgroundColor: 'rgba(59,130,246,0.04)',
                fill: true,
                tension: 0.4,
                borderWidth: 2.5,
                pointRadius: 5,
                pointBackgroundColor: '#fff',
                pointBorderColor: '#3b82f6',
                pointBorderWidth: 2.5,
                pointHoverRadius: 7
            }]
        },
        options: {
            responsive: true,
            maintainAspectRatio: false,
            plugins: {
                legend: { display: false },
                tooltip: {
                    backgroundColor: '#111827',
                    titleFont: { size: 12, weight: '700' },
                    bodyFont: { size: 13, weight: '600' },
                    padding: 12,
                    cornerRadius: 8,
                    callbacks: {
                        label: ctx => 'Rp ' + ctx.parsed.y.toLocaleString('id-ID')
                    }
                }
            },
            scales: {
                x: {
                    grid: { display: false },
                    ticks: { font: { size: 12, weight: '600' }, color: '#94a3b8' }
                },
                y: {
                    min: 0,
                    grid: { color: '#f3f4f6', borderDash: [4, 4] },
                    ticks: {
                        font: { size: 11, weight: '600' },
                        color: '#94a3b8',
                        callback: v => v >= 1000000 ? (v/1000000).toFixed(1) + 'M' : v >= 1000 ? (v/1000) + 'K' : v
                    }
                }
            }
        }
    });

    function copyLink() {
        const slug = document.getElementById('slugInput').value;
        navigator.clipboard.writeText('https://lingku.xyz/' + slug);
        alert('Link disalin!');
    }

    async function updateSlug() {
        const slug = document.getElementById('slugInput').value;
        if (!slug) return alert('Slug tidak boleh kosong.');
        
        try {
            const res = await fetch('/admin/update-slug', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ slug })
            });
            const d = await res.json();
            if (d.success) {
                alert('Slug berhasil diperbarui!');
                location.reload(); // Reload to sync header and other parts
            } else {
                alert(d.message || 'Gagal update slug.');
            }
        } catch(e) {
            alert('Terjadi kesalahan sistem.');
        }
    }
</script>
